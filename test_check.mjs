import { computeCheckInResults, calculateDimensionScore, getScoreLevel } from './src/lib/scoring.js';
// We can test the TypeScript logic directly via node by compiling or inline testing
console.log('Unit test script initialized');
